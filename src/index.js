import React from 'react';
import ReactDOM from 'react-dom';
import TodoContainer from './components/TodoContainer';

ReactDOM.render(<TodoContainer></TodoContainer>, document.getElementById('root'));
